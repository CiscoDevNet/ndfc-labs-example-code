# ndfc-labs-code
# This repo will hold all the examples of codes for coming NDFC learning labs including postman collection, ansible-playbook, and terraform configuration.

Install Ansible

Navigate to the Ansible code directory in the project:


cd /home/developer/devnet_ndfc/ndfc_ansible
Install Ansible and the NDFC collection for Ansible:


pip install ansible
ansible-galaxy collection install cisco.dcnm

# Nexus Dashboard Fabric Controller API
